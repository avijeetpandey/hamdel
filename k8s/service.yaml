apiVersion: v1
kind: Service
metadata:
  name: hamdel-ingress
  namespace: hamdel
  labels:
    app: hamdel
    role: ingress
spec:
  type: ClusterIP
  selector:
    app: hamdel
    role: ingress
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
